[project]
name = "out-of-town-games"
version = "0.1.1"
description = "emulates the scoreboards seen in ballparks like Boston, Chicago, Houston, Denver, etc.  "
requires-python = ">=3.11"
dependencies = [
    "flask",
    "requests",
]
